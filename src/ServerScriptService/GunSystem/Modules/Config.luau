local Config = {
    DefaultWalkSpeed = 16,
    CrouchSpeed = 8,
    ProneSpeed = 4,
    DefaultJumpHeight = 7.2,
    DefaultJumpPower = 50,  -- Used if the humanoid doesn't use JumpHeight
}

return Config